---
"mcp-effect-sdk": minor
---

Add an explicit MCP `2025-11-25` compatibility profile with pinned generated
schemas, stateful lifecycle and capability negotiation, bidirectional requests,
core Tasks, resource subscriptions, logging, stdio, and session-based Streamable
HTTP while retaining `2026-07-28` as the default.
