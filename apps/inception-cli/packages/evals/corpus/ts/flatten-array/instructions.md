# Flatten Array

Implement `flatten(arr: unknown[]): unknown[]`.

Recursively flattens arbitrarily nested arrays into a single flat array,
preserving element order. `null` and `undefined` values (nested or top-level)
are dropped entirely — they do not appear in the result, and empty nested
arrays contribute nothing.
