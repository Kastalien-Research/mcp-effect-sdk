# Matching Brackets

Implement `matched(s: string): boolean`.

Returns `true` iff every `(`, `[`, and `{` in `s` is closed by the matching
`)`, `]`, or `}` in the correct order (properly nested), `false` otherwise.
All other characters are ignored. The empty string is matched (`true`).
