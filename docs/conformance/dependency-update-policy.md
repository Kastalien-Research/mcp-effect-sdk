# Dependency Update Policy

The canonical published policy is
[DEPENDENCY_POLICY.md](../../DEPENDENCY_POLICY.md).

It covers pnpm lockfile ownership, the private `test/conformance` workspace,
exact `@modelcontextprotocol/conformance` pins, MCP source snapshots, update
cadence, and the required server/client/client-auth verification.
